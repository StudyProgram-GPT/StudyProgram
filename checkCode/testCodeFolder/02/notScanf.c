#include <stdio.h>
int main(int argc, char const *argv[])
{
    int a=-96, b=-47, c=78, d=57;
    int sum;
    //scanf("%d %d %d %d", &a, &b, &c, &d); 
    sum = c * d + a - b;
    printf("%d\n", sum);
    return 0;
}